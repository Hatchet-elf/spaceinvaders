


initinvasion(spaceshiparray);
return the array with all alienship drawn in
the x and y set to starting location
variable dead set to zero 

bool aliencollission(spaceshiparray, playerbullet);
return true if there is a collission
write the variable  for the dead to 1
set a variable in player bullet to ensure that the bullet no longer shows on the screen

int mvinvasion(spaceshiparray, direction);
move the x and y coords for each ship to the next location that will be drawn

int drawinvasion(spaceshiparray);
draw the alienship array

